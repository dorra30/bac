<form method="post" action="{{ path('app_bareme_garcon_delete', {'id': bareme_garcon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bareme_garcon.id) }}">
    <button class="btn">Delete</button>
</form>
