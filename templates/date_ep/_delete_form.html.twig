<form method="post" action="{{ path('app_date_ep_delete', {'id': date_ep.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ date_ep.id) }}">
    <button class="btn">Delete</button>
</form>
