{% extends 'base.html.twig' %}

{% block title %}BaremeFille{% endblock %}

{% block body %}
    <h1>BaremeFille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bareme_fille.id }}</td>
            </tr>
            <tr>
                <th>Performance</th>
                <td>{{ bareme_fille.performance }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ bareme_fille.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bareme_fille_index') }}">back to list</a>

    <a href="{{ path('app_bareme_fille_edit', {'id': bareme_fille.id}) }}">edit</a>

    {{ include('bareme_fille/_delete_form.html.twig') }}
{% endblock %}
