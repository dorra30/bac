<form method="post" action="{{ path('app_coordinateur_delete', {'id': coordinateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coordinateur.id) }}">
    <button class="btn">Delete</button>
</form>
